server:
  port: 8082

spring:
  application:
    name: voter-service

  datasource:
    url: jdbc:mysql://localhost:3306/e_voting_system
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true